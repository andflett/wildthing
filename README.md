# Wild Thing

Re-connecting kids with nature.

## Configuration

Requires a few ENV vars to be set if hosted on Heroku

 - mailchimp_api_key
 - mailchimp_api_list
 - s3_key_id
 - s3_secret_key
 - bucket